services:
  cli:
    build:
      context: .
      target: cli
      dockerfile: Dockerfile
    image: "laravel-trustup-io-slack-notifications-php-local"
    volumes:
      - ./:/opt/apps/app
    env_file:
      - .env
  bun:
    build:
      context: .
      target: bun
      dockerfile: Dockerfile
    image: "laravel-trustup-io-slack-notifications-bun-local"
    volumes:
      - ./:/opt/apps/app